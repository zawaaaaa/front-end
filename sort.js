function selectionSort(array);{
 for (i=0; i <array.lenght, i++){
 let aux= array [i];
   for(j=1; array.lengt;){
    if (array [i]> array [j]){
        array[i]=array[j]
        array [i+1]=aux;
      }   }        
    }
    return array;
}  
  let dado=[1,5,7,8];
console.log(selection
    Sort(dados)  );
